name: Sync IMDb Cookies to Cloudflare KV

on:
  workflow_dispatch: # Triggered by Cloudflare Worker Cron API or manually

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install Dependencies
        run: npm install --include=dev

      - name: Run Cloud Cookie Sync
        env:
          WORKER_URL: "https://meta.1proxy.workers.dev"
        run: node scripts/sync-cookies.mjs
